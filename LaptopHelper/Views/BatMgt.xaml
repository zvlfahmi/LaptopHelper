<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="LaptopHelper.Views.BatMgt"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LaptopHelper.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24">
        <StackPanel Spacing="20" Padding="32">
            <TextBlock Text="Battery Management" 
               Style="{StaticResource TitleTextBlockStyle}"/>

            <StackPanel 
                Background="{ThemeResource LayerOnAcrylicFillColorDefaultBrush}" 
                Padding="16" 
                CornerRadius="8" 
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                BorderThickness="1">

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Charge Remaining" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        <TextBlock x:Name="TxtChargeRemaining" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Status" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        <TextBlock x:Name="TxtBatteryStatus" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                    </StackPanel>
                </Grid>
            </StackPanel>

            <StackPanel Spacing="8">
                <TextBlock Text="BIOS Battery Health Manager" 
                   Style="{StaticResource SubtitleTextBlockStyle}"/>
                <ComboBox x:Name="CmbBatteryHealth" 
                  Width="300" 
                  PlaceholderText="Select option"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>